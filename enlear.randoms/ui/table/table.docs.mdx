---
labels: ['react', 'typescript', 'table']
description: 'A Table component.'
---

import { Table } from './table';

Table example:

- Please note that search and sort options have commented out due to a type error bug in react table library. But you can still run those options after clonning the project.

// live component playground - if you remove the word live it turns into a code snippet
```js live
<Table></Table>
```
